{% extends 'base.html.twig' %}

{% block title %}Editer un Article{% endblock %}

{% block body %}
    <h1>Editer un Article</h1>
    {% if is_granted('ROLE_ADMIN') %}
    {{ include('article/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('article/_delete_form.html.twig') }}
    {% else %}
        <h1> Vous n'avez pas les droits pour cette partie du site</h1>
    {% endif %}
    <a class="btn btn-info btn-sm" href="{{ path('article_index') }}">retour à la liste</a>

{% endblock %}
